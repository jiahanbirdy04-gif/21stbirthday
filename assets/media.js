/* ============================================================
   MEDIA CONFIG — auto-generated by studio.html.

   Don't want to touch code at all? Open studio.html, fill in your
   photos/video/audio/letter/music there, and it downloads a folder
   (including a new version of this exact file) ready to drop into
   the repo. This file is what you'd get from that.

   Prefer to edit by hand instead? Everything below is safe to edit
   directly too — it's plain data.
   ============================================================ */

// Generates a small inline placeholder image (no network request needed) so
// the site works immediately, with no external dependency. The Studio tool
// (studio.html) overwrites this whole file with your real photos, so you
// should rarely need to hand-edit this function.
function placeholderPhoto(n) {
  const palette = ["#EADCF8", "#D9C9F0", "#C7AEEF", "#E8D9F5", "#D6C4EE"];
  const c = palette[n % palette.length];
  const svg = `<svg xmlns='http://www.w3.org/2000/svg' width='400' height='500'>
    <rect width='100%' height='100%' fill='${c}'/>
    <text x='50%' y='52%' font-family='sans-serif' font-size='26' fill='#7B5BC4' text-anchor='middle'>photo ${n}</text>
  </svg>`;
  return "data:image/svg+xml;utf8," + encodeURIComponent(svg);
}

const MEDIA = {

  // ---- PHOTOS -------------------------------------------------
  // Don't hand-edit this — open studio.html, upload your real photos,
  // and it will regenerate this whole file for you automatically.
  photos: Array.from({ length: 12 }, (_, i) => ({
    src: placeholderPhoto(i + 1),
    caption: "",
  })),

  // ---- VIDEO ----------------------------------------------------
  // studio.html will place the uploaded video file at assets/video/
  // and fill this path in for you automatically.
  video: {
    src: "", // e.g. "assets/video/video.mp4"
    poster: "", // optional thumbnail shown before play
  },

  // ---- AUDIO: Happy Birthday clip (finale) -----------------------
  happyBirthdayAudio: {
    src: "", // public URL to a short royalty-free/purchased "Happy Birthday" clip
  },

  // ---- MUSIC PAGE -------------------------------------------------
  music: {
    featured: {
      title: "PLACEHOLDER — Featured Track Title",
      artist: "Artist Name",
      embedUrl: "", // e.g. a Spotify/YouTube embed URL
    },
    tracklist: [
      { title: "Track 1 — placeholder", artist: "Artist" },
      { title: "Track 2 — placeholder", artist: "Artist" },
      { title: "Track 3 — placeholder", artist: "Artist" },
      { title: "Track 4 — placeholder", artist: "Artist" },
      { title: "Track 5 — placeholder", artist: "Artist" },
    ],
  },

  // ---- FLOWERS: affirmation notes ----------------------------------
  flowerNotes: [
    "You make every room warmer just by being in it.",
    "Twenty-one years of you, and the world's better for all of them.",
    "Here's to the person who always shows up for everyone else — today's for you.",
    "Still the easiest person to laugh with, no matter how far apart we are.",
    "So proud of who you're becoming. Keep going.",
  ],

  // ---- LETTER -------------------------------------------------------
  letter: {
    body:
`PLACEHOLDER — Haki's letter to Noy goes here.

Replace this text with the real message. It can be as long as you want —
the page scrolls, and the serif type is set to stay easy to read even
for a longer letter.

Happy 21st, Noy.

— Haki`,
  },
};

// explicit, in case anything ever references window.MEDIA directly
window.MEDIA = MEDIA;
